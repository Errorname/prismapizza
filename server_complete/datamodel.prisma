type Pizza {
  id: ID! @unique
  name: String!
  description: String!
  img: String!
  price: Float!
}

type Catalogue {
  pizzas: [Pizza!]!
}

type Order {
  id: ID! @unique
  pizzas: [Pizza!]!
  total: Float!
}
