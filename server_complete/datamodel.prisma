type Pizza {
  id: ID! @id
  name: String!
  description: String!
  img: String!
  price: Float!
}

type Catalogue {
  id: ID! @id
  pizzas: [Pizza!]!
}

type Order {
  id: ID! @id
  pizzas: [Pizza!]!
  total: Float!
}
